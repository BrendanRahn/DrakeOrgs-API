

services:
  lambda:
    image: "lambda_image"
    ports:  
      - "9000:8080"
    environment:
      # - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID} # make these secret
      # - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION}
